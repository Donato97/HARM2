{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities di default per la finestra principale",
  "windows": ["main"],
  "permissions": ["core:default", "axum:default"]
}
